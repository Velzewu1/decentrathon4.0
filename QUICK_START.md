# 🚀 БЫСТРЫЙ ЗАПУСК LLM PUSH ГЕНЕРАТОРА

## Шаг 1: Установите зависимости

```bash
pip install -r requirements.txt
```

## Шаг 2: Настройте API ключ

Создайте файл `.env` в корне проекта:

```bash
# Создайте файл .env
echo "OPENAI_API_KEY=your_openai_api_key_here" > .env
```

**ВАЖНО:** Замените `your_openai_api_key_here` на ваш настоящий API ключ OpenAI!

Получить ключ можно здесь: https://platform.openai.com/api-keys

Пример правильного .env файла:
```
OPENAI_API_KEY=sk-proj-abcd1234efgh5678ijkl9012mnop3456qrst...
```

## Шаг 3: Запустите генерацию

```bash
python run_llm_pipeline.py
```

## ✅ Что происходит:

1. 📊 Загружает данные 60 клиентов из `data/`
2. 🔧 Рассчитывает 75+ признаков поведения
3. 💰 Вычисляет benefit scores для 11 продуктов
4. 📈 Ранжирует и выбирает лучший продукт для каждого клиента
5. 🤖 Генерирует персонализированные push через OpenAI GPT-4o-mini
6. 📊 Валидирует качество (цель: 80%+)
7. 💾 Сохраняет результат в `output/recommendations_llm_generated.csv`

## 🎯 Ожидаемый результат:

```
🎉 LLM ПАЙПЛАЙН ЗАВЕРШЕН УСПЕШНО!
📊 Качество push: 95.0%
📏 Средняя длина: 196 символов  
✅ Валидных push: 57/60
💾 Результат: output/recommendations_llm_generated.csv
🏆 ПРЕВОСХОДНЫЙ РЕЗУЛЬТАТ! Качество превышает 80%
```

## 🔍 Пример сгенерированного push:

```csv
client_code,product,push_notification
1,Кредитная карта,"Айгерим, у вас разные категории трат, а кредитная карта даст до 10% кешбэка на любимые покупки и беспроцентный период. Оформить карту? Оформление займёт всего пару минут в мобильном приложении."
```

## ❗ Возможные проблемы:

**Ошибка: API ключ не найден**
- Убедитесь, что файл `.env` создан в корне проекта
- Проверьте, что ключ заменен на настоящий (не `your_openai_api_key_here`)

**Ошибка: модуль не найден**  
- Выполните: `pip install -r requirements.txt`

**Медленная работа**
- Генерация 60 push занимает 1-2 минуты - это нормально
- LLM делает retry и пост-обработку для качества 95%

## 💰 Стоимость:
- Около $0.50 за 60 клиентов
- GPT-4o-mini — очень экономичная модель

---

**🏆 Готово к хакатону и production использованию!**
