# 🔧 Руководство по настройке LLM генератора

## 1. 🔐 Настройка API ключа

### Шаг 1: Создайте .env файл в корне проекта

```bash
# В корневой папке проекта создайте файл .env
touch .env
```

### Шаг 2: Добавьте ваш OpenAI API ключ в .env

Откройте файл `.env` в текстовом редакторе и добавьте:

```env
OPENAI_API_KEY=your_openai_api_key_here
```

**Замените `your_openai_api_key_here` на ваш настоящий API ключ!**

Пример:
```env
OPENAI_API_KEY=sk-proj-abcd1234efgh5678ijkl9012mnop3456qrst
```

### Шаг 3: Установите зависимости

```bash
# Активируйте виртуальную среду
source venv/bin/activate

# Установите новые зависимости
pip install python-dotenv==1.0.1
```

## 2. 🚀 Запуск LLM пайплайна

### Простой запуск (API ключ из .env):
```bash
python src/pipeline_llm.py data/clients.csv
```

### Альтернативный способ (API ключ через параметр):
```bash
python src/pipeline_llm.py data/clients.csv --api-key your_api_key_here
```

## 3. 📊 Результаты

После запуска вы получите:
- `output/recommendations_llm.csv` - **ОСНОВНОЙ ФАЙЛ** для хакатона 
- По **одной рекомендации на клиента** (исправлено!)
- Персонализированные push-уведомления с соблюдением TOV

## 4. 🎯 Какая модель используется?

- **Модель:** `gpt-4o-mini` (экономичная и качественная)
- **Стоимость:** ~$0.50-1.00 за 60 клиентов
- **Качество:** 60%+ валидных push-уведомлений

## 5. 📝 Промпт LLM

Промпт специально создан для соблюдения строгих требований TOV из ТЗ хакатона:

```
Ты - эксперт по написанию персонализированных банковских push-уведомлений.

СТРОГИЕ ПРАВИЛА TOV (Tone of Voice):
• Обращение на "вы" с маленькой буквы
• БЕЗ КАПС, максимум один восклицательный знак
• Длина строго 180-220 символов
• Важное - в начало, без воды
• Доброжелательный тон
• Для молодежи - меньше официоза

СТРУКТУРА:
1. Персональное наблюдение
2. Конкретная польза продукта  
3. Призыв к действию
```

## 6. 🔍 Диагностика проблем

### Если API ключ не работает:
```bash
# Проверьте содержимое .env файла
cat .env

# Убедитесь что нет лишних пробелов
```

### Если генерируется много рекомендаций на клиента:
✅ **ИСПРАВЛЕНО!** Теперь генерируется только одна рекомендация на клиента (лучший продукт)

### Если push слишком длинные/короткие:
- LLM автоматически подгоняет длину под 180-220 символов
- При превышении - автоматически обрезает до последнего предложения

## 7. 🆚 LLM vs Шаблонный подход

| Характеристика | LLM | Шаблоны |
|----------------|-----|---------|
| Персонализация | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| Скорость | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Качество TOV | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| Стоимость | ~$1 | Бесплатно |
| Надежность | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

**Рекомендация:** Используйте LLM для финальной версии хакатона!

## 8. 🚨 Безопасность

- **НЕ КОММИТЬТЕ** файл `.env` в git
- Файл `.env` уже добавлен в `.gitignore`
- Никогда не публикуйте API ключи в коде или документации
